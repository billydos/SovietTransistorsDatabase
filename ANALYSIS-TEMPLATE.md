# ANALYSIS-NN — <тип анализа> <объект анализа>

<!-- Замените NN на порядковый номер отчёта (01, 02, ...). Тип анализа: Code Review, аудит безопасности, анализ производительности, анализ данных и т.п. -->

- **Дата:** ГГГГ-ММ-ДД
- **База:** коммит `<хеш>` (+ незакоммиченные правки: <файлы или «нет»>)
- **Объём:** <~N строк Go / количество файлов / охватываемые пакеты>
- **Метод:** <как проводился анализ: чтение исходников, какие команды выполнялись, какие сценарии воспроизводились, какие инструменты использовались>

## Резюме

<2–4 абзаца: общее состояние объекта анализа; главные выводы; в каких местах концентрируются проблемы (с указанием номеров находок); что проверено и что осталось за рамками. Пишите по существу — это единственный раздел, который читают все.>

## Сводная таблица находок

<!-- Одна строка на находку. Приоритеты: Высокий (A — данные/корректность/заявленные свойства), Средний (B — надёжность, UX, сопровождаемость), Низкий (C — стиль, производительность, документация). «Воспроизведено»: Да / Нет / По коду (обоснование, почему воспроизведение невозможно или не требуется). -->

| ID | Приоритет | Область | Суть | Воспроизведено |
|----|-----------|---------|------|----------------|
| A1 | Высокий | <файл/пакет> | <суть проблемы одним предложением> | <Да/Нет/По коду> |
| B1 | Средний | <файл/пакет> | <суть> | <Да/Нет/По коду> |
| C1 | Низкий | <файл/пакет> | <суть> | <Да/Нет/По коду> |

<!-- Группы C-находок допустимо объединять в одну строку со ссылкой на раздел C. -->

---

## A. Находки высокого приоритета

### A1. <Формулировка проблемы — что именно не так и к чему это приводит>

**Где:** `<файл:строки-строки>` (<функция/тип>), <связанные места: заявки в документации, вызовы, инварианты>.

<Существо проблемы: что происходит, почему это неверно, кому вредит. Ссылки на правила AGENTS.md/SKILL.md, если находка им противоречит. Если воспроизводилось — пошаговый сценарий воспроизведения с фактическим результатом и ожидаемым.>

**Рекомендация:** <конкретное исправление с вариантами; если решение нетривиальное — этапы. Минимум — как зафиксировать ограничение (комментарий, тест).>

<!-- Повторяйте блок «### AN» для каждой находки группы. -->

---

## B. Находки среднего приоритета

### B1. <Формулировка>

**Где:** `<файл:строки-строки>`.

<Описание, сценарий воспроизведения (если применим), последствия.>

**Рекомендация:** <исправление.>

---

## C. Находки низкого приоритета, стиль и производительность

<!-- Мелкие находки — таблицей, без отдельных подразделов. -->

| ID | Где | Описание |
|----|-----|----------|
| C1 | `<файл:строка>` | <что и почему; при необходимости — оценка значимости.> |

---

## Что сделано хорошо

<!-- Отдельный раздел для сильных сторон — чтобы при исправлениях их не сломали и не «оптимизировали» осознанные решения. -->

- **<Решение/паттерн>** (`<файл:строки>`): <почему это хорошо>.
- <Ещё пункты.>

## Выполненные проверки

<!-- Все команды и сценарии, фактически выполненные в ходе анализа, с результатами. Подтверждает воспроизведённые находки и базовое состояние. -->

| Проверка | Результат |
|----------|-----------|
| `go build ./...` | <результат> |
| `go vet ./...` | <результат> |
| `gofmt -l .` | <результат> |
| `go test ./...` | <результат> |
| Сценарий <описание> | <результат → ссылка на ID находки> |

## Рекомендуемый порядок исправлений

<!-- Нумерованный список от самого дешёвого/видимого к сложному/спорному; укажите зависимости между правками (например, тесты до рефакторинга). -->

1. **<ID>** (<почему первым>) — <ожидаемый эффект>.
2. **<ID>** ...
